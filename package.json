{
  "name": "chainlist-sample",
  "version": "0.0.1",
  "description": "",
  "main": "truffle.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "truffe": "truffle",
    "truffle-ganache": "truffle migrate --network ganache",
    "truffle-ganache-reset": "truffle migrate --compile-all --reset --network ganache",
    "truffle-console": "truffle console --network ganache",
    "truffle-test": "truffle test --network ganache",
    "dev": "lite-server"
  },
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.3.0"
  },
  "dependencies": {
    "solc": "^0.4.18",
    "truffle": "^4.0.4",
    "web3": "^0.20.0"
  }
}
